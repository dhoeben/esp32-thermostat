interval:
  - interval: 60s
    then:
      - switch.turn_on: adc_enable
      - delay: 200ms       # allow LDO + SHT45 to stabilise
      - component.update: sht4x_sensor
      - delay: 100ms
      - switch.turn_off: adc_enable
      - component.update: epd 